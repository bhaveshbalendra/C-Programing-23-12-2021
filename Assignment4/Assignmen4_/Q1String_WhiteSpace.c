	
//Program to read and display a string with white spaces
#include <stdio.h>

void main()
{
    char str[20];
    printf("Enter any string");
    gets(str);
    printf("\n The entered string is:");
    puts(str);
}
